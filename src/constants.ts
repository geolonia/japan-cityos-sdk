
export const DEFAULT_CIRCLE_COLOR = '#FF0000';
export const DEFAULT_LINE_COLOR = '#0000FF';
export const DEFAULT_FILL_COLOR = '#00FF00';
export const DEFAULT_MARKER_NAME = 'default-marker';
export const DEFAULT_TEXT_COLOR = '#000000';

export const LARGE_MARKER_SIZE = 1;
export const MEDIUM_MARKER_SIZE = 0.5;
export const SMALL_MARKER_SIZE = 0.3;

export const LARGE_CIRCLE_RADIUS = 10;
export const MEDIUM_CIRCLE_RADIUS = 6;
export const SMALL_CIRCLE_RADIUS = 3;

export const DEFAULT_FILL_OPACITY = 0.5;

export const DEFAULT_LINE_WIDTH = 2;
export const BOLD_LINE_WIDTH = 4;

export const MARKER_SIZE_MAP: Record<string, number> = {
  small: SMALL_MARKER_SIZE,
  medium: MEDIUM_MARKER_SIZE,
  large: LARGE_MARKER_SIZE,
};

export const CIRCLE_SIZE_MAP: Record<string, number> = {
  small: SMALL_CIRCLE_RADIUS,
  medium: MEDIUM_CIRCLE_RADIUS,
  large: LARGE_CIRCLE_RADIUS,
};

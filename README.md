# Japan Maps SDK

地図を表示する
```
const japanMap = new geolonia.japan.Map({
    container: 'map',
    lngLat: [],
    zoom: 12,
})
```

- minzoom、maxzoomのセット

- 背景地図の切り替え（名前を指定 or URL指定して）

- symbolデータのアイコンを変更する

- 緯度経度を受け取って、その範囲にある地物の属性情報を返す

https://geolonia.github.io/cityos-sdk/index.js
